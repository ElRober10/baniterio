// Configuración de PRODUCCIÓN (la que usa `ng build` por defecto).
// URL relativa: asume que el front y el backend se sirven desde el mismo
// dominio detrás de un proxy. En desarrollo, angular.json sustituye este
// fichero por environment.development.ts (que apunta a localhost:8080).
export const environment = {
  apiBaseUrl: '/api/v1',
};
